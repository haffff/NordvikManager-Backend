Run dotnet build and see warnings that are occurring during the build process.
This will help identify any potential issues in the code that may not be causing errors but could lead to problems in the future.

- Fix the warnings in the codebase that don't require significant changes.
- For warnings that require significant changes, create a plan and create low priority issues to address them in the future.